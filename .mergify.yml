pull_request_rules:
  - name: Automatic merge for main when CI passes and 1 review
    conditions:
      - author=AbyelT
      - check-success=build 
      - base=main
    actions:
      merge:
        method: merge
